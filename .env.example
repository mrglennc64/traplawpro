# Passcodes for unlocking the attorney portal.
# These never ship to the browser — checked server-side in /api/attorney-unlock.
ATTORNEY_PASSCODE=change-me-attorney
ADMIN_PASSCODE=change-me-admin

# Base URL used to build signing links from the Digital Handshake form.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
